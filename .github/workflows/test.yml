name: Test

on:

permissions:
  contents: read
  security-events: write
  actions: read

  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Test
      run: echo "✅ Repository test passed"